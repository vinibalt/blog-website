<%- include('partials/header') %>


    <h1>Home</h1>
    <p>
        <%= homeParagraph %>
    </p>

    <% posts.forEach((e)=> { %>
        <h3>
            <a href="posts/<%= e.title %> ">
                <%= e.title %>
            </a>
        </h3>
        <p>
            <%= (e.body).slice(0, 100) + '...' %>
                <a href="posts/<%= e.title %> ">Read More</a>
        </p>
        <% }) %>



            <%- include('partials/footer') %>